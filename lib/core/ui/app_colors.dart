import 'package:flutter/material.dart';

class AppColors {
  static const brand = Color(0xFF276572);
  static const appBackground = Colors.white;
  // Restore full brand gradient on auth/onboarding shells
  static const authShellTop = brand;

  const AppColors._();
}
